# COLLIDE 20260921T0848Z

*6 pairs drawn by `research/collide.py`. One question each. Most of these are nothing -- that is the expected yield, not a bug. Record what came of each with `python research/collide.py --verdict <id> <id> nothing|lead`.*

**Is there a MEASUREMENT that connects these two? Not a story -- a number we could go and compute from data we already hold. If yes, say what it is and what would have to be true for it to be an artefact. If no, say 'nothing' and move on.**

## 1. F-B01 x F-C01   *(Bankroll and risk x The contract)*

- **F-B01** — Size follows the bank automatically, re-read every 300 seconds, and only while flat.
- **F-C01** — A new 15-minute window opens every 15 minutes, 24/7, on 12 crypto series.

## 2. F-B06 x F-I05   *(Bankroll and risk x Infrastructure and data)*

- **F-B06** — At the current close-loss rate a back-to-back pair is a 3.0% event per day, 19.5% per week, 60.6% per month.
- **F-I05** — A collector restart inside a UTC hour leaves two gzip members and the standard reader recovers ZERO lines from that file, not half.

## 3. F-B03 x F-I09   *(Bankroll and risk x Infrastructure and data)*

- **F-B03** — The worst one close can lose is exactly `MAX_PER_CLOSE * size * PRICE_CEILING` — a bound, not a sample maximum.
- **F-I09** — `pinlevels_rows.jsonl` holds 16,683 candidates, of which 2,699 are ones the live bot REFUSES (dump guard).

## 4. F-I05 x F-P11   *(Infrastructure and data x Our performance)*

- **F-I05** — A collector restart inside a UTC hour leaves two gzip members and the standard reader recovers ZERO lines from that file, not half.
- **F-P11** — No coin is reliably safer: rank correlation between halves is +0.35 and the rule fails non-monotonically out of sample.

## 5. F-M02 x F-X03   *(The model x The exchange)*

- **F-M02** — `sd/sigma` collapses far faster than `sqrt(tau)` — 9.7x too large at `tau = 10` if you use `sqrt(tau)`.
- **F-X03** — A crossing IOC limit fills at the RESTING order's price, not at our limit. 283/283 live fills at or below the signalled price; 76 strictly better; ZERO worse.

## 6. F-P01 x F-P04   *(Our performance x Our performance)*

- **F-P01** — Break-even is a **3.58%** close-loss rate. A win pays ~3.7c, a loss costs ~96c: one loss undoes ~27 wins.
- **F-P04** — It is not our speed: filled and zero-filled orders have the SAME median latency, 96 ms both.

