# COLLIDE 20260925T0847Z

*6 pairs drawn by `research/collide.py`. One question each. Most of these are nothing -- that is the expected yield, not a bug. Record what came of each with `python research/collide.py --verdict <id> <id> nothing|lead`.*

**Is there a MEASUREMENT that connects these two? Not a story -- a number we could go and compute from data we already hold. If yes, say what it is and what would have to be true for it to be an artefact. If no, say 'nothing' and move on.**

## 1. F-I01 x F-X07   *(Infrastructure and data x The exchange)*

- **F-I01** — The settlement index (`cfbenchmarks_value`) arrives once per second.
- **F-X07** — Order round trip from this box is a median 96 ms, p90 116 ms.

## 2. F-B05 x F-P09   *(Bankroll and risk x Our performance)*

- **F-B05** — The loss brake counts losing CLOSES, not losing trades. Three stops the run.
- **F-P09** — Removing the depth floor costs 30% of the money: +15% fills, -35% mean size, -25% contracts.

## 3. F-I06 x F-X01   *(Infrastructure and data x The exchange)*

- **F-I06** — Kalshi renamed fields once and 68,976,084 of 68,976,084 deltas went unparsed while every stage exited 0.
- **F-X01** — The price tick is tapered: 0.1c below 10c and above 90c, 1c in between.

## 4. F-C07 x F-I09   *(The contract x Infrastructure and data)*

- **F-C07** — Fair value's sensitivity to the volatility estimate is exactly ZERO at 50c.
- **F-I09** — `pinlevels_rows.jsonl` holds 16,683 candidates, of which 2,699 are ones the live bot REFUSES (dump guard).

## 5. F-M02 x F-O04   *(The model x The other side)*

- **F-M02** — `sd/sigma` collapses far faster than `sqrt(tau)` — 9.7x too large at `tau = 10` if you use `sqrt(tau)`.
- **F-O04** — 83.3% of the time there is NO ask at any price on the side the model wants. Of those, 100% carry an ask on the LOSING side — somebody is quoting, and will not quote the side we want.

## 6. F-C03 x F-C06   *(The contract x The contract)*

- **F-C03** — The strike is the same average over the sixty seconds before the OPEN.
- **F-C06** — With `tau` seconds left, `60 - tau` of the settlement prints are already on disk and unchangeable.

