# LEADS 20260917

**PROPOSALS. Nothing here was measured — this session holds no data.**

## 1. F-M08 x F-O07 — did the late tape hand us more of the bad kind of window?

- **F-M08** — within each coin the lowest-volatility fifth is the worst arm (5.19% bad, 43.7x overconfident).
- **F-O07** — the counterparty did not change early-to-late; only the model's accuracy did.

**Measurement.** The candidate bad rate doubled (1.87% -> 4.61%) and why is the
top open question. Coin mix is ruled out (8 of 9 coins worsened alone) and
drift is ruled out (calibration flat). Volatility mix INSIDE each coin is not.
Join `pinlevels_rows.jsonl` (gated `live`) to `pinflood`'s `rv_short` at
close-60s and split the change in two: more candidates landing in the dangerous
fifth, versus the same fifth getting worse.

**Artefact.** Cut the quintile edges ONCE on the early period and apply them
late. Re-cutting inside each era forces 20% into every fifth and erases the mix
by construction — the whole answer lost to a convention. Second: a candidate
exists only where somebody quoted (F-O04: no ask 83.3% of the time), so a shift
in WHO quotes moves the mix with the index unchanged. F-O07 measured contest,
speed and depth; it does not cover that.

**MDE.** The mix half needs no outcomes and is well powered. The within-fifth
half is not — ~13,984 rows, design effect 31-46, five fifths by two eras. State
it first and expect no power, not no effect.

**Cost.** Both inputs cached. Minutes.

## 2. F-M06 x F-M08 — why the volatility multiplier failed

- **F-M06** — multiplying volatility by 1.25 keeps 44% of candidates and makes
  them WORSE (2.90% -> 3.09%).
- **F-M08** — the lowest-volatility fifth is the worst arm.

**Measurement.** Multiplying sd by k divides every z by k, so the filter keeps
the highest-z rows — and z is high when sd is small. Cross-tab the k=1.25 and
k=2.0 survivors against `rv_short` fifth. If they pile into the bottom fifth, k
failed by COMPOSITION, not because "the error is shape, not width", which is
the reason `CURRENT_STATE` currently records. The test that separates them:
inside one fifth, k should do nothing.

**Artefact.** Rank within coin (F-M09). F-M08's ordering was found in-sample;
`pinflood`'s holdout already shows `rv_short` holding (0.28x -> 0.14x), so
confirming it on the candidate population is nearly free — do it first.

**Cost.** One cross-tab on cached rows. Minutes.
