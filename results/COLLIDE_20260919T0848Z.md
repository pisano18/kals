# COLLIDE 20260919T0848Z

*6 pairs drawn by `research/collide.py`. One question each. Most of these are nothing -- that is the expected yield, not a bug. Record what came of each with `python research/collide.py --verdict <id> <id> nothing|lead`.*

**Is there a MEASUREMENT that connects these two? Not a story -- a number we could go and compute from data we already hold. If yes, say what it is and what would have to be true for it to be an artefact. If no, say 'nothing' and move on.**

## 1. F-B01 x F-R04   *(Bankroll and risk x Rules we measured our way into)*

- **F-B01** — Size follows the bank automatically, re-read every 300 seconds, and only while flat.
- **F-R04** — Every large edge this project has produced has been a measurement bug.

## 2. F-B03 x F-X04   *(Bankroll and risk x The exchange)*

- **F-B03** — The worst one close can lose is exactly `MAX_PER_CLOSE * size * PRICE_CEILING` — a bound, not a sample maximum.
- **F-X04** — The order body has no "no" side: `price` is always the YES price. Buying NO at q is `side="ask", price=1-q`.

## 3. F-M07 x F-R01   *(The model x Rules we measured our way into)*

- **F-M07** — Where nobody offered to sell, the model misses its own claim by 0.028pp. Where somebody did, by 0.725pp — **26x**.
- **F-R01** — Twelve series settle on the same quarter hour at rho ~0.8 — worth ~1.22 independent observations per close, not 12.

## 4. F-B02 x F-M04   *(Bankroll and risk x The model)*

- **F-B02** — `size = bank / (BANK_BRAKE * MAX_PER_CLOSE * PRICE_CEILING)`; at BANK_BRAKE 3.0 that is `bank / 5.88`.
- **F-M04** — It is actually wrong 2.90% of the time on that population — **19x** its own claim.

## 5. F-I07 x F-R04   *(Infrastructure and data x Rules we measured our way into)*

- **F-I07** — `discover()` skips an unknown ticker with no error and no log line.
- **F-R04** — Every large edge this project has produced has been a measurement bug.

## 6. F-M02 x F-M07   *(The model x The model)*

- **F-M02** — `sd/sigma` collapses far faster than `sqrt(tau)` — 9.7x too large at `tau = 10` if you use `sqrt(tau)`.
- **F-M07** — Where nobody offered to sell, the model misses its own claim by 0.028pp. Where somebody did, by 0.725pp — **26x**.

