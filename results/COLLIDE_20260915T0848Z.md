# COLLIDE 20260915T0848Z

*6 pairs drawn by `research/collide.py`. One question each. Most of these are nothing -- that is the expected yield, not a bug. Record what came of each with `python research/collide.py --verdict <id> <id> nothing|lead`.*

**Is there a MEASUREMENT that connects these two? Not a story -- a number we could go and compute from data we already hold. If yes, say what it is and what would have to be true for it to be an artefact. If no, say 'nothing' and move on.**

## 1. F-I03 x F-X02   *(Infrastructure and data x The exchange)*

- **F-I03** — 57.7 million trade records over 376 hours; ~5% fall in the last 60 seconds of a quarter hour.
- **F-X02** — Makers pay no fee. Takers pay `0.07 * p * (1-p)`, billed per ORDER and ceilinged to $0.0001.

## 2. F-I10 x F-M03   *(Infrastructure and data x The model)*

- **F-I10** — The watchdog only checks liveness every 300 seconds, so anything killed costs up to five minutes of unrecoverable tape.
- **F-M03** — The model states a confidence. Over the live-gate candidate population it claims it will be wrong 0.15% of the time.

## 3. F-B01 x F-R02   *(Bankroll and risk x Rules we measured our way into)*

- **F-B01** — Size follows the bank automatically, re-read every 300 seconds, and only while flat.
- **F-R02** — Measured on the candidate population, that rho is 0.62-0.69 and the design effect is 31-46x.

## 4. F-P10 x F-X05   *(Our performance x The exchange)*

- **F-P10** — Market-making at the touch earns +0.48c per fill, t=+6.4, on 17.1M fills — and takers there carry ZERO information (t=0.2). Never turned into dollars because queue position is unmeasured.
- **F-X05** — Sweeps print per level — 59% of same-instant groups, median 8 legs.

## 5. F-M02 x F-R03   *(The model x Rules we measured our way into)*

- **F-M02** — `sd/sigma` collapses far faster than `sqrt(tau)` — 9.7x too large at `tau = 10` if you use `sqrt(tau)`.
- **F-R03** — Cross-strike arbitrage is UNDEFINED here, not merely absent — there is one strike per window.

## 6. F-P04 x F-P07   *(Our performance x Our performance)*

- **F-P04** — It is not our speed: filled and zero-filled orders have the SAME median latency, 96 ms both.
- **F-P07** — Selling a winner before settlement always loses money. By 15s out a loser's bid is already 23c, 6c at 10s.

