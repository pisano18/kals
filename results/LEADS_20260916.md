# LEADS 20260916

**PROPOSALS. Nothing here was measured — this session holds no data.**

## 1. F-P01 x F-R04 — an interval on break-even

- **F-P01** — break-even close-loss rate: 3.58% in FACTS.md, recomputed to 8.55%.
- **F-R04** — every large edge here has been a measurement bug.

**Measurement.** `b = W/(W+|L|)` moved 2.4-5.3x in our favour with no new trade —
F-R04's shape — and `|L|` rests on TEN closes, worst -$52.60 against a mean of $14.55.
Bootstrap `b` over losing closes clustered by close TIME. Then normalise per ENTRY
contract and re-weight to today's size: F-P06 has hedge recovery decaying with size,
so `|L|` grows faster than `W` and the 8.55 -> 19.11 "improvement" should shrink.

**Data.** Our own live fills and settlements (`pinver`'s source), never the tape —
rule 5. Minutes.

**MDE.** At 10 losing closes the bootstrap on `|L|` is roughly ±50%, putting `b` near
[5%, 20%] against our loss rate's [2.09%, 7.78%]. Likely unresolvable at this n — no
power, not no effect. One more -$52 close takes `b` to 7.02%, under the 7.78% the
95%-confidence claim leans on.

**Artefact.** `--loss-abort` and `--max-losses` truncate the tail, biasing `b` HIGH —
flag rail-hit closes, report both ways. Size grew because we won, so band by size,
never against time. Count entry contracts only; hedge legs dilute a loss for free.

## 2. F-I09 x F-R02 — a refusal on one coin as news about another

- **F-I09** — 2,699 of 16,683 candidate rows are dump-guard REFUSALS.
- **F-R02** — on that population rho is 0.62-0.69, design effect 31-46x.

**Measurement.** Rho is usually the tax; here it is the resource. The dump guard fires
on 2c offers the model calls certain — a marker the index just jumped. Ask whether a
firing on ANY coin strictly BEFORE the scan second raises the bad rate on the coins we
trade at that close.

**Data.** Re-score `pinlevels_rows.jsonl` through `pincontest.gate(rows,"live")` —
seconds, no tape walk. A candidate bad rate is what the market did, NOT our loss rate.

**MDE.** ~13,984 gated rows, ~368 effective observations at deff 38; split two ways
that is about **+5 pp** on a 2.9% base. Only a ~2.7x lift is findable.

**Artefact.** F-O05's leak in new dress: a firing at or after our entry instant leaks
the outcome — strict pre-entry timestamps. At rho 0.65 a positive association is
guaranteed, so the null is what rho implies (`endgame.redraw_null`), not zero.
