# memory_snapshot

A copy of the Claude Code auto-memory folder
(`C:\Users\Joe\.claude\projects\C--kals-repo\memory\`) taken 2026-09-26 ~07:30Z
at the end of the 09-24..26 session, so a session on another machine (or after
that folder is lost) can read it from GitHub. The live copy is the folder above;
this snapshot is NOT updated automatically. `MEMORY.md` is the index; each other
file is one fact (feedback, project state, operator context or a reference).

Start with `next-session-first-jobs.md`, then the top of `../HANDOFF.md`
("PICK UP HERE").
